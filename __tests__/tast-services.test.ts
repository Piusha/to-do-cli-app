describe('TaskService', () => {

  it('should validate task before saving', () => {

    // const taskService  =  new TaskService();
    // const task = {
    //   title: 'task title',
    //   description: 'task description',
    //   status: 'OPEN',
    // };

    // const result = taskService.validate(task);

    expect(true).toBe(true);

  });
});
